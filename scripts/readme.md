I am currently not using these files.
They are supposed to cache notions files using CLI commands.
See the CLI commands at package.json
It should work with ./tmp folder and ./src/lib/notion/client.ts
